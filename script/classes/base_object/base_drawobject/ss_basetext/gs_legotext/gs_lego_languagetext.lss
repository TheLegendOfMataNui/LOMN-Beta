class gs_lego_languagetext : gs_legotext {
    property stringtable;
    property stringindex;
    
    method gs_lego_languagetext(font_name, string_index, stringtable, x_pos, y_pos, z_pos, scale, horizontal_alignment, vertical_alignment, padding, is_new_font) {
        var var1;
        this.stringtable = stringtable;
        this.stringindex = string_index;
        var1 = this.stringtable.languagearray[this.stringindex];
        this.gs_legotext(font_name, var1, x_pos, y_pos, z_pos, scale, horizontal_alignment, vertical_alignment, padding, is_new_font);
        return null;
    }
    
    method setstring(stringindex, stringtable) {
        this.stringindex = stringindex;
        if (stringtable != null) {
            this.stringtable = stringtable;
        }
        this.string = this.stringtable.languagearray[this.stringindex];
        sctextobject::setstring(this.text, this.string);
        return null;
    }
}