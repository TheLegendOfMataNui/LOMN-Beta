function process(param1, param2, param3) {
    var var1;
    if (!(screenlist == null)) {
        var1 = new base_standardprocess(param1, param2, param3);
        screenlist.process(var1);
        screenlist.safecallback();
        globalclass.process(var1);
    }
    else {
        globalclass = null;
    }
    return null;
}